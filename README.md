# Курсовая работа - Система рейтинга игр

Веб-приложение для оценки и рейтинга видеоигр с административной панелью.

## 📋 Описание проекта

Проект состоит из трех основных компонентов:
- **Frontend** - React приложение для пользователей
- **Backend** - Node.js/Express сервер с REST API
- **Admin Panel** - C++ консольное приложение для администраторов

## 🛠️ Требования

Перед установкой убедитесь, что у вас установлены следующие программы:

### Обязательные зависимости:

1. **Node.js** (версия 16 или выше)
   - Скачать: https://nodejs.org/
   - Проверка установки: `node --version`

2. **npm** (устанавливается вместе с Node.js)
   - Проверка установки: `npm --version`

3. **MySQL** (версия 8.0 или выше)
   - Скачать: https://dev.mysql.com/downloads/mysql/
   - Или использовать XAMPP: https://www.apachefriends.org/

4. **C++ компилятор** (для Admin Panel)
   - **Windows**: MinGW-w64 или Visual Studio
   - **Linux**: `sudo apt-get install build-essential g++`
   - **macOS**: Xcode Command Line Tools (`xcode-select --install`)

5. **Git** (для клонирования репозитория)
   - Скачать: https://git-scm.com/downloads

## 📥 Установка

### Шаг 1: Клонирование репозитория

Откройте терминал (или командную строку) и выполните:

```bash
git clone https://github.com/ваш-username/Klivan49.git
cd Kurshach_OOOP
```

### Шаг 2: Настройка базы данных MySQL

1. Запустите MySQL сервер
2. Создайте базу данных:

```sql
CREATE DATABASE kursach CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

3. Импортируйте схему базы данных:

```bash
mysql -u root -p kursach < kursach.session.sql
```

Или откройте MySQL Workbench и выполните SQL скрипт `kursach.session.sql`

### Шаг 3: Установка зависимостей Backend

Перейдите в папку `server` и установите зависимости:

```bash
cd server
npm install
```

### Шаг 4: Настройка переменных окружения Backend

Создайте файл `.env` в папке `server` со следующим содержимым:

```env
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=ваш_пароль_mysql
DB_NAME=kursach
JWT_SECRET=ваш_секретный_ключ_для_jwt
NODE_ENV=development
```

**Важно**: Замените `ваш_пароль_mysql` на ваш пароль от MySQL и `ваш_секретный_ключ_для_jwt` на случайную строку (например, сгенерированную через `openssl rand -base64 32`)

### Шаг 5: Установка зависимостей Frontend

Откройте новый терминал и перейдите в папку `kursach-react`:

```bash
cd kursach-react
npm install
```

### Шаг 6: Сборка Admin Panel (опционально)

Если вы хотите использовать C++ Admin Panel:

#### Windows (MinGW):

1. Убедитесь, что MySQL установлен и пути в `Makefile` правильные
2. Откройте терминал в папке `AdminPanel`
3. Выполните:

```bash
make
```

#### Linux/macOS:

```bash
cd AdminPanel
make
```

**Примечание**: Если у вас проблемы с путями к MySQL, отредактируйте `AdminPanel/Makefile` и укажите правильные пути к заголовочным файлам и библиотекам MySQL.

## 🚀 Запуск приложения

### Запуск Backend сервера

В терминале в папке `server`:

```bash
# Для разработки (с автоперезагрузкой)
npm run dev

# Или для production
npm start
```

Сервер запустится на порту 3000 (или на порту, указанном в переменной окружения `PORT`).

### Запуск Frontend приложения

В новом терминале в папке `kursach-react`:

```bash
npm start
```

Приложение откроется в браузере по адресу `http://localhost:3000` (или другой порт, если 3000 занят).

### Запуск Admin Panel

В терминале в папке `AdminPanel`:

```bash
# Windows
admin_panel.exe

# Linux/macOS
./admin_panel
```

## 📦 Зависимости проекта

### Backend (server/package.json):
- **express** ^4.18.2 - веб-фреймворк
- **mysql2** ^3.6.5 - драйвер для MySQL
- **bcryptjs** ^2.4.3 - хеширование паролей
- **jsonwebtoken** ^9.0.2 - JWT токены для аутентификации
- **cors** ^2.8.5 - поддержка CORS
- **dotenv** ^16.3.1 - загрузка переменных окружения
- **nodemon** ^3.0.2 (dev) - автоперезагрузка при разработке

### Frontend (kursach-react/package.json):
- **react** ^19.2.1 - библиотека React
- **react-dom** ^19.2.1 - рендеринг React
- **react-scripts** 5.0.1 - скрипты Create React App
- **axios** ^1.13.2 - HTTP клиент
- **firebase** ^12.6.0 - интеграция с Firebase
- **web-vitals** ^2.1.4 - метрики производительности

### Admin Panel:
- **MySQL Client Library** - библиотека для подключения к MySQL
- **C++17 компилятор** (g++ или совместимый)

## 🔧 Устранение неполадок

### Проблема: "Cannot find module"
**Решение**: Убедитесь, что вы выполнили `npm install` в соответствующих папках (`server` и `kursach-react`)

### Проблема: Ошибка подключения к базе данных
**Решение**: 
- Проверьте, что MySQL сервер запущен
- Убедитесь, что данные в `.env` файле правильные
- Проверьте, что база данных `kursach` создана

### Проблема: Порт уже занят
**Решение**: Измените порт в настройках или остановите процесс, использующий порт

### Проблема: Ошибка компиляции Admin Panel
**Решение**:
- Проверьте, что MySQL установлен
- Убедитесь, что пути в `Makefile` соответствуют вашей установке MySQL
- Для Windows: проверьте, что MinGW установлен и добавлен в PATH

## 📁 Структура проекта

```
Kurshach_OOOP/
├── server/                 # Backend (Node.js/Express)
│   ├── config/            # Конфигурация базы данных
│   ├── middleware/        # Middleware (аутентификация)
│   ├── routes/            # API маршруты
│   ├── migrations/        # SQL миграции
│   ├── server.js          # Главный файл сервера
│   └── package.json       # Зависимости backend
├── kursach-react/         # Frontend (React)
│   ├── src/              # Исходный код
│   ├── public/           # Публичные файлы
│   └── package.json      # Зависимости frontend
├── AdminPanel/           # C++ Admin Panel
│   ├── src/             # Исходный код C++
│   ├── headers/         # Заголовочные файлы
│   ├── Makefile         # Файл сборки
│   └── config.json      # Конфигурация
├── kursach.session.sql  # SQL схема базы данных
├── .gitignore           # Игнорируемые файлы Git
└── README.md           # Этот файл
```

## 👥 Автор

Klivan

## 📄 Лицензия

MIT

## 🔗 Полезные ссылки

- [Документация Node.js](https://nodejs.org/docs/)
- [Документация React](https://react.dev/)
- [Документация Express](https://expressjs.com/)
- [Документация MySQL](https://dev.mysql.com/doc/)

